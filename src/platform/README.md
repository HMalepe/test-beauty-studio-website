# Platform (`src/platform/`)

**Only code that is genuinely identical across all clients** — e.g. onboarding placeholders, unknown-tenant fallback.

Do not add client branding, headers, heroes, or section components here.
